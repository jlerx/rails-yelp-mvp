<div class="container">
  <h1>All restaurants:</h1>
  <% @restaurants.each do |restaurant| %>
    <div class="card">
      <%= link_to restaurant.name, restaurant_path(restaurant) %>
    </div>
  <% end %>
  <div class="card">
    <strong><%= link_to 'Add a restaurant', new_restaurant_path %></strong>

  </div>
</div>